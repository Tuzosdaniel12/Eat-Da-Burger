<li class="list-group-item d-flex justify-content-between align-items-center">
    {{name}}
    
    <span class="btn btn-danger badge  rounded-pill" data-id="{{id}}">
        {{#unless devoured}}
            DEVOUR
        {{#unless devoured}}

        {{#if devoured}}
            REMOVE
        {{#if devoured}}
    </span>
</li>


     