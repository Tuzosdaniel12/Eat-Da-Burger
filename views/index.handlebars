<div class="container">
        <header class="row justify-content-md-center position-relative">
            <img src="images/burger.jfif"  class="burger-img"alt="burger">

            <h1 class="text-center ">EAT THE BURGER!</h1>
        </header>

        <form class="input-group form" id="burger_textarea">
            <input type="text" class="form-control" placeholder="Burger" aria-label="Recipient's Burger" id="add-burger"></input>
        </form>

        <div class="container p-2 text-center" id="burger-list">
            <div class="row">
              <div class="col-sm p-2">
                  <h3>Eat Now</h3>
                <ul class="list-group">
                    {{#each burger}}
                        {{#unless devoured}}
                            {{> burgers/burger-block devoured=true}}
                        {{/unless}}
                    {{/each}}
                </ul>
              </div>

              <div class="col-sm p-2">
                  <h3>Devoured</h3>
                <ul class="list-group">
                    {{#each burger}}
                        {{#if devoured}}
                            {{> burgers/burger-block devoured=false}}
                        {{/if}}
                    {{/each}} 
                </ul>
              </div>
            </div>
          </div>
</div>